<%= form_for(@artist) do |f| %>
  <label>Edit the Artist name: </label>
  <%= f.text_field :name %><br>
  <label>Edit the Artist bio: </label>
  <%= f.text_field :bio %><hr>
  <%= f.submit%><hr>
<% end %>
